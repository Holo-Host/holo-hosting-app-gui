
# -----------  Agents  -----------
[[agents]]
id = "holo_user1"
name = "HoloHostTester1"
public_address = "HcScJPHedTj3wqwwxeX8feBuWXr93onx4TY5xKjdz3Dotsb5bDoxjn75WMw6h4z"
key_file = "/home/lisa/.config/holochain/keys/HcScJPHedTj3wqwwxeX8feBuWXr93onx4TY5xKjdz3Dotsb5bDoxjn75WMw6h4z"

# [[agents]]
# id = "test_agent2"
# name = "HoloTester2"
# public_address = "sandwich--------------------------------------------------------------------------AAAEqzh28L"
# key_file = "holo_tester.key"

# [[agents]]
# id = "test_agent3"
# name = "HoloTester3"
# public_address = "bagel-----------------------------------------------------------------------------AAADAq0mCu"
# key_file = "holo_tester.key"

# -----------  DNA  -----------
[[dnas]]
id = "holohost"
file = "dnas/HoloHostingApp.json"
hash = "Qm328wyq38924y"

[[instances]]
id = "holo-hosting-app_instance"
dna = "holohost"
agent = "holo_user1"
[instances.logger]
type = "simple"
file = "app_spec1.log"
[instances.storage]
type = "file"
path = "tmp-storage"

# [[instances]]
# id = "agent2_dna_instance"
# dna = "holohost"
# agent = "test_agent2"
# [instances.logger]
# type = "simple"
# file = "app_spec2.log"
# [instances.storage]
# type = "file"
# path = "tmp-storage"

[[interfaces]]
id = "websocket_interface"
[interfaces.driver]
type = "websocket"
port = 3000
[[interfaces.instances]]
id = "holo-hosting-app_instance"
# [[interfaces.instances]]
# id = "agent2_dna_instance"
# -----------  UI  -----------
[[ui_bundles]]
id = "main"
root_dir = "ui-holohost"
hash = "Qmjk0"

[[ui_interfaces]]
id = "ui-interface"
bundle = "main"
port = 8800
dna_interface = "websocket_interface"


# [[ui_bundles]]
# id = "main2"
# root_dir = "./ui-agent2"
# hash = "Qm000"
#
# [[ui_interfaces]]
# id = "ui-interface2"
# bundle = "main2"
# port = 9300
# dna_interface = "websocket_interface"

# -----------  Networking  -----------
[network]
n3h_path = "/home/lisa/Documents/gitrepos/holochain/n3h"
n3h_persistence_path = "/tmp"
bootstrap_nodes = []

# -----------  Networking instructions  -----------
# To get networking running requires the following steps:

# - Clone the repo for the holochain networking library n3h (https://github.com/holochain/n3h)
# - Checkout the tag in n3h for holochain v0.0.4-alpha (git fetch --tags && git checkout v0.0.4-alpha)
# - run `npm install && npm run boostrap` in the n3h directory
# - change the n3h_path above to point to the location where you cloned the repo

# - If you are the first node then start the conductor and take careful note of the line that
#	begins with `READY! tcp://127.0.0.1:43859 [...]
#	You will need to copy the contents of the array and send it to the nodes you plan to network with.
# - If you are a joining node then paste the contents from the first node in to the bootstrap_nodes field above
# - Make sure that you all have different agents for your instances (uncomment new agent and change the instances agent field)
# - Uncomment the network section and you should be ready do go!
